# Archive

These ADRs record completed migrations or reversed decisions. They are
historical: none of them encode a constraint the current codebase must
satisfy. Active ADRs live one level up, in `docs/adr/`.
